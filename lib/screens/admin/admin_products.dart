import 'package:flutter/material.dart';

class AdminProducts extends StatelessWidget {
  const AdminProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Admin Products', style: TextStyle(fontSize: 50)),
    );
  }
}
